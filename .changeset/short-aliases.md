---
'@cpane/cli': minor
---

Add short aliases for every subcommand: `l`/`ls` for `list`, `a` for `add`, `e` for `edit`, `s` for `sync`, `i` for `shell-init`, `u` for `upgrade`, `r` for `replicate-profiles`.
