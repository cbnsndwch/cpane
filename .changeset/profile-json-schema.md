---
'@cpane/cli': minor
---

Add `cpane schema` (alias `sc`), which writes a JSON Schema for profile files to your config dir — point your editor's JSON/YAML language server at it for autocomplete and structural validation while hand-editing profiles.
