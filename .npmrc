# Keep one flat lockfile at the repo root; do not let installs invent nested ones.
shared-workspace-lockfile=true
# Surface peer issues as warnings, never hard-fail install.
strict-peer-dependencies=false
# Deterministic engine behavior across contributors.
engine-strict=false
